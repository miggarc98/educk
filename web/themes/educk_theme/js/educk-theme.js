/**
 * @file
 * educk_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.educkTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
